<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blacksw.mapper.boardMapper">	
	
	<insert id="inserBoard" parameterType="com.blacksw.board.dto.BoardDTO">
		INSERT 
		INTO private_board_service.board(title, content, uid)
		VALUES (#{title}, #{content}, #{uid})
	</insert>

</mapper>